// example config:
// "Settings"
// {
//    // load from this config rather than autosave.cfg on plugin start
//    "defaultconfig" "addons/sourcemod/configs/attribute_manager/rebalance.cfg"
//}

"Settings"
{
    // load from this config rather than autosave.cfg on plugin start
    "defaultconfig" "" // Leave blank to default to autosave.cfg
}